---
export interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-6">
  <h2 class="text-2xl font-semibold tracking-tight">{title}</h2>
  {subtitle ? <p class="mt-2 max-w-2xl text-sm text-slate-600">{subtitle}</p> : null}
</div>
