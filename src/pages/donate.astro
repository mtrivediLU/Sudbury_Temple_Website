---
import Layout from "../layouts/Layout.astro";
import SectionHeading from "../components/ui/SectionHeading.astro";

/**
 * DONATE PAGE (based on your provided copy)
 * Notes:
 * - CanadaHelps link: you should replace the placeholder with the official URL once confirmed.
 * - Address / phone: keep as text guidance. Put the final official values on Contact page as the source of truth.
 */

const charity = {
  registeredName: "Sudbury Prarthana Samaj",
  registrationNumber: "119205078 RR 0001",
};

const donationHelps = [
  "Temple maintenance, utilities, and supplies",
  "Worship services and weekly gatherings",
  "Festival celebrations and special pujas",
  "Community meals or prasad",
  "Accessibility and facility improvements",
  "Programs that support children, youth, and community learning",
];

const howToDonate = [
  {
    title: "Donate online via CanadaHelps",
    desc: "Online donations are accepted via CanadaHelps. You will receive a donation receipt based on the platform process.",
    actionLabel: "Donate through CanadaHelps",
    href: "#",
    note: "Replace this link with the official CanadaHelps donation page URL once confirmed.",
    primary: true,
  },
  {
    title: "Questions about donations",
    desc: "If you have a specific donation purpose or you want to confirm receipting details, contact us and we will guide you.",
    actionLabel: "Contact us",
    href: "/contact",
    note: "You can mention your preferred purpose, date, or any sponsorship intent in your message.",
    primary: false,
  },
];

const transparencyNotes = [
  {
    title: "Receipts and records",
    desc: "Please include your name and email so your donation receipt and confirmation details can be handled correctly.",
  },
  {
    title: "Purpose and notes",
    desc: "If you want your donation to support a specific program or event, include a short note when donating or contact us.",
  },
  {
    title: "Community impact",
    desc: "Your support helps keep the space welcoming and accessible, and strengthens worship, culture, and community service in Northern Ontario.",
  },
];
---

<Layout
  title="Sudbury Prarthna Samaj | Donate"
  description="Donate to support Sudbury Prarthana Samaj. Your donation supports temple maintenance, worship services, festivals, and community initiatives."
>
  <!-- HERO -->
  <section class="mx-auto max-w-6xl px-4 pb-6 pt-10 md:pt-12">
    <div class="inline-flex items-center rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">
      Donate
    </div>

    <h1 class="mt-4 text-3xl font-semibold tracking-tight text-slate-900 md:text-4xl">
      Support the temple and community
    </h1>

    <p class="mt-4 max-w-3xl text-sm leading-relaxed text-slate-700 md:text-base">
      Your donation supports temple maintenance, worship services, festivals, community meals or prasad,
      and improvements that keep the space welcoming and accessible.
    </p>

    <div class="mt-6 flex flex-wrap gap-3">
      <a
        href="#"
        class="rounded-lg bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white hover:bg-slate-800"
      >
        Donate through CanadaHelps
      </a>
      <a
        href="/contact"
        class="rounded-lg border border-slate-200 px-5 py-2.5 text-sm font-semibold text-slate-900 hover:bg-slate-50"
      >
        Contact for donation questions
      </a>
    </div>
  </section>

  <!-- WHY DONATE -->
  <section class="mx-auto max-w-6xl px-4 py-12">
    <SectionHeading
      title="Why donate"
      subtitle="Your support sustains worship, programs, and the temple space."
    />

    <div class="grid gap-6 md:grid-cols-2">
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="text-sm font-semibold text-slate-900">How donations help</div>

        <ul class="mt-4 space-y-2 text-sm text-slate-700">
          {donationHelps.map((x) => (
            <li class="flex gap-3">
              <span class="mt-1 h-2 w-2 rounded-full bg-slate-400"></span>
              <span>{x}</span>
            </li>
          ))}
        </ul>

        <div class="mt-6 rounded-xl bg-slate-50 p-4 text-sm text-slate-700">
          Registered Charity. Charity Registration Number: <span class="font-semibold">{charity.registrationNumber}</span>
        </div>
      </div>

      <div class="rounded-2xl border border-slate-200 bg-slate-50 p-6 shadow-sm">
        <div class="text-sm font-semibold text-slate-900">Charity details</div>

        <div class="mt-4 space-y-3 text-sm text-slate-700">
          <div class="rounded-xl bg-white p-4 ring-1 ring-slate-200">
            <div class="text-xs font-semibold text-slate-600">Registered Charity Name</div>
            <div class="mt-1 font-semibold text-slate-900">{charity.registeredName}</div>
          </div>

          <div class="rounded-xl bg-white p-4 ring-1 ring-slate-200">
            <div class="text-xs font-semibold text-slate-600">Charity Registration Number</div>
            <div class="mt-1 font-semibold text-slate-900">{charity.registrationNumber}</div>
          </div>

          <div class="rounded-xl bg-white p-4 text-sm text-slate-700 ring-1 ring-slate-200">
            If you see a different charity name or number on any platform, please let the temple committee know so it can be corrected.
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- HOW TO DONATE -->
  <section class="mx-auto max-w-6xl px-4 py-12">
    <SectionHeading
      title="How to donate"
      subtitle="Online donations are accepted via CanadaHelps."
    />

    <div class="grid gap-6 md:grid-cols-2">
      {howToDonate.map((x) => (
        <div class={`rounded-2xl p-6 shadow-sm ${x.primary ? "bg-slate-900 text-white" : "border border-slate-200 bg-white text-slate-900"}`}>
          <div class={`text-sm font-semibold ${x.primary ? "text-white" : "text-slate-900"}`}>{x.title}</div>
          <p class={`mt-3 text-sm ${x.primary ? "text-white/85" : "text-slate-700"}`}>{x.desc}</p>

          <a
            href={x.href}
            class={`mt-5 inline-block rounded-lg px-5 py-2.5 text-sm font-semibold ${
              x.primary
                ? "bg-white text-slate-900 hover:bg-slate-100"
                : "bg-slate-900 text-white hover:bg-slate-800"
            }`}
          >
            {x.actionLabel}
          </a>

          <p class={`mt-4 text-xs ${x.primary ? "text-white/70" : "text-slate-600"}`}>
            {x.note}
          </p>
        </div>
      ))}
    </div>

    <div class="mt-8 rounded-2xl border border-slate-200 bg-slate-50 p-6 shadow-sm">
      <div class="text-sm font-semibold text-slate-900">Donation note</div>
      <p class="mt-2 text-sm text-slate-700">
        If you are donating for a specific purpose (festival support, prasad, facility improvements, or a special prayer),
        include a short note when donating or send us a message with the details.
      </p>
      <a href="/contact" class="mt-4 inline-block text-sm font-semibold text-slate-900 hover:underline">
        Contact us to share a donation purpose
      </a>
    </div>
  </section>

  <!-- TRANSPARENCY / FAQ -->
  <section class="mx-auto max-w-6xl px-4 pb-16 pt-4">
    <div class="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm">
      <SectionHeading
        title="Good to know"
        subtitle="A few details that help keep donations simple and transparent."
      />

      <div class="mt-6 grid gap-4 md:grid-cols-3">
        {transparencyNotes.map((t) => (
          <div class="rounded-2xl bg-slate-50 p-5 ring-1 ring-slate-200">
            <div class="text-sm font-semibold text-slate-900">{t.title}</div>
            <p class="mt-2 text-sm text-slate-700">{t.desc}</p>
          </div>
        ))}
      </div>

      <div class="mt-8 rounded-2xl bg-slate-900 p-7 text-white">
        <div class="text-lg font-semibold">Need help donating?</div>
        <p class="mt-2 text-sm text-white/85">
          If you have questions about the donation method, receipting, or a specific purpose, contact us and we will assist.
        </p>
        <div class="mt-5 flex flex-wrap gap-3">
          <a
            href="/contact"
            class="rounded-lg bg-white px-5 py-2.5 text-sm font-semibold text-slate-900 hover:bg-slate-100"
          >
            Contact us
          </a>
          <a
            href="/"
            class="rounded-lg bg-white/10 px-5 py-2.5 text-sm font-semibold text-white ring-1 ring-white/20 hover:bg-white/15"
          >
            Back to home
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
