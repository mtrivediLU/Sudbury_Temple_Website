---
import Layout from "../layouts/Layout.astro";
import SectionHeading from "../components/ui/SectionHeading.astro";
---

<Layout title="Sudbury Prarthna Samaj | Contact">
  <section class="mx-auto max-w-6xl px-4 py-12">
    <SectionHeading
      title="Contact"
      subtitle="For bookings, membership, volunteering, donations, and general inquiries."
    />

    <div class="grid gap-6 md:grid-cols-2">
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="text-sm font-semibold">Email</div>
        <p class="mt-2 text-sm text-slate-700">
          <a class="font-semibold text-slate-900 hover:underline" href="mailto:sudbury.mandir@gmail.com">
            sudbury.mandir@gmail.com
          </a>
        </p>

        <div class="mt-6 text-sm font-semibold">What to include</div>
        <ul class="mt-2 list-disc space-y-2 pl-5 text-sm text-slate-700">
          <li>Purpose (booking, donation, membership, volunteer)</li>
          <li>Preferred dates and time</li>
          <li>Your contact details</li>
        </ul>
      </div>

      <div class="rounded-2xl bg-slate-50 p-6">
        <div class="text-sm font-semibold">Next improvement</div>
        <p class="mt-2 text-sm text-slate-700">
          If you want a website form submission (instead of email), the clean approach is to use Formspree or a similar service.
          We can add that in the next iteration.
        </p>
      </div>
    </div>
  </section>
</Layout>
